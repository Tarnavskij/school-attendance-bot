# core/constants.py
"""
Единый источник правды для констант, которые используются в нескольких хендлерах.

ABSENCE_REASONS — порядок здесь определяет порядок кнопок при выборе причины
(handlers/my_class.py) и порядок строк в сводке секретаря (handlers/secretary.py).
Меняй список только здесь — оба хендлера импортируют именно отсюда.
"""

ABSENCE_REASONS: list[str] = [
    "🤒 По болезни",
    "📄 По заявлению",
    "🏖 На оздоровлении",
    "❓ Без уважительной причины",
]

# Причина по умолчанию, если она не указана (reason is None) — используется
# в агрегации по школе (repositories.get_absence_reason_counts).
DEFAULT_ABSENCE_REASON = "❓ Без уважительной причины"